#
# Podfile
#

platform :ios, '11.0'
inhibit_all_warnings!
use_frameworks!

target 'MovieDB' do
end

target 'MovieDBTests' do
end

# Keys

plugin 'cocoapods-keys', {
  project: 'MovieDB',
  keys: [
    'MovieDatabaseAPIKey'
  ]
}
